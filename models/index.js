// === EXPORT MODELS ======================================================================

// Both models will be exported here as an object for access from my server
module.exports = {
    Article: require("./Article"),
    Note: require("./Note")
};